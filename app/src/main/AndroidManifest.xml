<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_pokeball"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_pokeball"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstudiantesCRUD"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".activities.EditarPokemonActivity"
            android:exported="false"
            android:parentActivityName=".activities.MiPokedexActivity" />

        <activity
            android:name=".activities.DetallePokemonActivity"
            android:exported="false"
            android:parentActivityName=".activities.ExplorarActivity" />

        <activity
            android:name=".activities.MiPokedexActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.ExplorarActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
